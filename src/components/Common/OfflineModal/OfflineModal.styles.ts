import styled from 'styled-components';

export const OfflineModal = styled.div`
  background-color: ${props => props.theme.mainColors.primary};
  border: 1px solid ${props => props.theme.mainColors.border};
`;
