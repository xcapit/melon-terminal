type Query {
  network: String!
  block: Int!
  accounts: [Account]!
}

type Account {
  id: ID!
  address: String
  balance: Float
}

type Subscription {
  counter: Int
}

type Schema {
  query: Query
  subscription: Subscription
}